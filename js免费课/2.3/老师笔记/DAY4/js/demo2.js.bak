var ary = [12,24,34,10,8,5];
ary.sort(function(a,b){
    return a-b; //[5, 8, 10, 12, 15, 16, 24, 34]
});
console.log(ary);

/*1.冒泡排序
* 实现原理:
* 当 进行两两比较 当前项比后一项要大 两者交换位置
* 最后是最大的放在最后;
* [12,24,34,10,8,5]   -> 6个
* 第一轮: 次数 5次 (不用跟自己比)
* 12,24   [12,24,34,10,8,5]
* 24,34   [12,24,34,10,8,5]
* 34,10   [12,24,10,34,8,5]
* 34,8    [12,24,10,8,34,5]
* 34,5    [12,24,10,8,5,34]
* 第二轮 ： 次数 3次  (不用跟自己比  也不需要跟最后的一个数 )
* 24,10   [12,10,24,8,5,34]
* 24,8    [12,10,8,24,5,34]
* 24,5    [12,10,8,5,24,34]
*
*
* 要进行比较的轮数 ary.length-1
* 每一轮要进行比较的次数 ary.length - 1 - i
* */

function bubble(ary){
    // 外层循环是比较的轮数：最后一个不需要拿出来跟后面的比较
    for (var i = 0; i < ary.length -1; i++) {
        //里层循环是每次比较的次数 (不用跟自己比 不用跟第一轮过后的最后一个比较)
        for (var j = 0; j < ary.length -1-i; j++) {
             if(ary[j] > ary[j+1]){
                 /* 当前j 大于 后一项 j+1*/
                var temp = ary[j];
                 ary[j] = ary[j+1];
                 ary[j+1] = temp;
             }
        }
    }
    return ary;
}
bubble([12,24,34,10,8,5]);  //5 8 10 12 24 34


/*var a = 34;
var b = 10;
var temp = a;
a = b;
b = temp;*/
